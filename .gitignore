*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out
*.tf
.git
*vendor

# local dev workspace (SDK companion checkout)
go.work
go.work.sum
